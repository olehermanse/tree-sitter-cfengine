bundle agent my_bundle
{
  vars:
    "foo"
@if minimum_version(3.18)
      string => call(variable, call("bar"));
@else
      if => { @{variable} },
      string => call(variable, call("bar"));
@endif

  reports:
    "foo";
}
