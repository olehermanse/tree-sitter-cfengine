bundle agent main
{
  vars:
    "list"
      if => isvariable(variable),
      slist => { @(expand_list) };
    "string"
      string => $(expand_string);
    "string"
      string => $($(expand_string));
}
