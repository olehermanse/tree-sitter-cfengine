==================
Class guards
==================

bundle agent my_bundle
{
  vars:
    "foo"
      string => "bar",
      if => "any";
}

---

(source_file
  (bundle_block
    (bundle_keyword)
    (bundle_type)
    (bundle_name)
    (bundle_body
      (bundle_section
        (promise_guard)
        (promise
          (promiser)
          (attribute
            (attribute_name)
            (right_value
              (quoted_string)))
          (attribute
            (attribute_name)
            (right_value
              (quoted_string))))))))
