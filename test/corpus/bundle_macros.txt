==================
Only macros
==================

@if minimum_version(3.18)
bundle agent my_bundle {}
@else
bundle agent their_bundle {}
@endif

---

(source_file
  (macro)
  (bundle_block
    (bundle_keyword)
    (bundle_type)
    (bundle_id)
    (bundle_body))
  (macro)
  (bundle_block
    (bundle_keyword)
    (bundle_type)
    (bundle_id)
    (bundle_body))
  (macro))
